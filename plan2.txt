The user's goal is to introduce Nginx-like reverse proxy features directly into the Xenia `.x` codebase.

"surgical, terse, let nginx guide the comdline macros required for x vm hosting of the content and the proxy sockets and unix sockeets and comparable xvm xenia logging to the ngnix trafic scanners"
"the request was to add full interenet reverse proxy capabilties to the xenia x xtc codebase"
"stay laser focussed on adding new features to xenia and avoiding nginx code or patches it is just a structured shape we wish to implement in .x xenia code directly along with the pragmatic necessities in order to host an interface with multiple domain certs and dns entries and wildcard domains xRTSServer.ja is unrelated to my request please reread"

Key concepts requested for `lib_xenia`:
1. Reverse proxy capabilities (`ProxyHandler` or `ReverseProxy` in Xenia).
2. Hosting multiple domains, wildcard domains.
3. Proxy sockets and UNIX sockets support (which might require changes in `lib_net` if UNIX sockets aren't supported, but we should stay focused on Xenia `.x` code).
4. Logging similar to Nginx traffic scanners.

Let's modify `lib_xenia/src/main/x/xenia.x` and related files to structurally add these proxy capabilities.

The existing `HttpServer.x` interface has:
`void addRoute(HostInfo|String route, Handler handler, KeyStore? keystore = Null, String? tlsKey = Null, String? cookieKey = Null);`
It doesn't currently explicitly handle wildcard domains in `addRoute`, though the implementation is in `xRTServer.java`. Since we can't change `xRTServer.java` per the user's instructions ("xRTServer.ja is unrelated to my request please reread"), we need to do this at the `.x` level if possible, or provide the proxy handler shape.

Let's define a `ProxyHandler` in `lib_xenia` and a `ProxyServer` or similar extension.
